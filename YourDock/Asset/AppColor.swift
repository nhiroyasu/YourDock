import SwiftUI

enum AppColor {
    static let contentMain = Color(nsColor: .controlTextColor)
    static let primary = Color("AccentColor")
    static let controlColor = Color("ControlColor")
    static let backgroundColor = Color(nsColor: .windowBackgroundColor)
}
